# 날씨 상세 및 캐릭터 아웃핏 시스템 SDD + TDD

> 문서 상태: 구현 전 확정안  
> 기준일: 2026-08-03 (KST)  
> 1차 MVP 대상: 서울 1개 도시  
> 범위: 설계와 테스트 명세만 포함하며, 이 문서 작성 단계에서 애플리케이션 코드는 변경하지 않는다.

---

## 1. 문서 목적

현재 도시별 날씨 카드의 `alert` 상세보기를 다음 경험으로 발전시키기 위한 설계를 확정한다.

- 도시 카드 전체를 누르면 날씨 상세 패널이 열린다.
- 패널 상단에서 캐릭터가 현재 날씨에 맞는 옷과 소품을 착용한다.
- 사용자에게 수치보다 먼저 `무엇을 입고 챙겨야 하는지`를 안내한다.
- 기상청·에어코리아·한국천문연구원 데이터를 서버에서 통합하여 Vue에 제공한다.
- 특정 외부 소스가 실패해도 가능한 데이터로 화면을 유지한다.

### 1.1 `실시간` 정의

본 문서에서 실시간은 매초 갱신을 의미하지 않는다. 각 제공기관의 **가장 최근 관측·발표 자료**를 호출하고, 화면에 `데이터 기준 시각`과 `앱이 갱신한 시각`을 구분해 보여주는 것을 의미한다.

---

## 2. 목표와 비목표

### 2.1 목표

1. 사용자가 도시 카드를 누른 후 5초 안에 외출 준비를 결정할 수 있다.
2. 캐릭터의 아웃핏, 행동 문구, 시간별 예보가 동일한 날씨 상태를 표현한다.
3. 데이터 일부가 없거나 늦어도 정상 데이터까지 숨기지 않는다.
4. 외부 API 인증키가 브라우저에 노출되지 않는다.
5. 1차 MVP는 서울로 제한하되 다른 도시를 추가하기 쉬운 계약을 사용한다.

### 2.2 비목표

- 기상 예측 모델을 직접 만들지 않는다.
- 사용자의 의학적 질환에 따른 개인화 조언은 MVP에 포함하지 않는다.
- 전국 18개 도시 연동, 위치 자동 탐지, 푸시 알림, 레이더 지도는 MVP 이후로 미룬다.
- 캐릭터 옷이 물리 시뮬레이션되는 실시간 3D는 범위에서 제외한다.

---

## 3. 사용자 시나리오

### US-01 도시 상세 열기

- 사용자는 `상세보기` 버튼을 정확히 누르지 않고 서울 카드 어디든 눌러 상세 패널을 연다.
- 삭제 버튼은 카드 상세 열기와 분리된다.
- 키보드 사용자는 `Enter` 또는 `Space`로 열 수 있다.

### US-02 오늘의 준비 파악

- 사용자는 캐릭터 옷차림과 `오늘은 햇빛을 단단히 막아요` 같은 문구를 먼저 본다.
- 사용자는 준비물 칩에서 선크림, 양산, 우산 등을 확인한다.

### US-03 비 시간 파악

- 사용자는 `맑음/비` 라벨만이 아니라 비가 시작·종료되는 예상 시간, 강수확률, 예상 강수량을 확인한다.

### US-04 데이터 상태 파악

- 사용자는 언제 발표된 데이터인지 확인한다.
- 일부 데이터가 없으면 `점검 중`, `관측 정보 없음` 등으로 이유를 안내받는다.

---

## 4. UX 및 화면 설계

### 4.1 상세 진입 방식

| 화면 크기 | 표시 방식 | 닫기 방식 |
| --- | --- | --- |
| 767px 이하 | 화면 아래에서 올라오는 바텀시트 | 닫기, 배경 클릭, `Esc`, 아래 스와이프(후순위) |
| 768px 이상 | 오른쪽 사이드 패널 | 닫기, 배경 클릭, `Esc` |

패널을 열 때 포커스를 패널 제목으로 이동하고, 닫을 때 원래 누른 도시 카드로 복귀시킨다. 모달 방식이면 뒤의 콘텐츠에 포커스가 가지 않도록 한다.

### 4.2 정보 계층

1. **출처·갱신 상태**: 서울, 데이터 기준 시각, 새로고침
2. **캐릭터 히어로**: 아웃핏, 현재 기온·상태, 행동 헤드라인
3. **준비물 칩**: 우산, 선크림, 양산, 물, 마스크 중 최대 3개
4. **시간별 타임라인**: 향후 6시간은 초단기, 이후는 단기 예보
5. **상황별 주요 카드**: 비 예보 시 강수, 맑은 날 UV, 대기질 나쁨 시 PM2.5를 먼저 표시
6. **보조 지표**: 습도, 풍속, 일출·일몰

### 4.3 상태별 화면

| 상태 | 히어로 | 본문 |
| --- | --- | --- |
| 초기 | 카드 아웃라인 스켈레톤 | 타임라인·지표 스켈레톤 |
| 전체 성공 | 캐릭터 + 행동 문구 | 모든 데이터 |
| 부분 성공 | 가용 데이터로 캐릭터 결정 | 실패 지표만 `정보 없음` |
| 캐시 사용 | 정상 표시 + `이전 정보` 라벨 | 마지막 성공 시각 표시 |
| 전체 실패 | 중립 기본 캐릭터 | 재시도 버튼 + 이전 목업 데이터는 실제처럼 표시하지 않음 |
| 수치 무효 | 유효한 조건만 반영 | 무효 항목은 `확인 불가` |

### 4.4 접근성

- 카드는 `button` 요소 또는 동등한 키보드 역할을 사용한다.
- 캐릭터 아웃핏은 장식이 아닌 정보이므로 대체 텍스트를 제공한다. 예: `방수 재킷과 장화를 입고 우산을 든 캐릭터`.
- 위험 등급은 색상만으로 표현하지 않고 `높음`, `나쁨` 텍스트를 병기한다.
- `prefers-reduced-motion` 사용자에게 캐릭터·비 애니메이션을 정지한다.
- 터치 대상은 최소 44×4444px로 한다.

---

## 5. 데이터 소스 설계

### 5.1 출처

| 제공자 | 용도 | MVP 필수 | 갱신·주의 |
| --- | --- | --- | --- |
| 기상청 단기예보 조회서비스 | 초단기실황, 초단기예보, 단기예보 | 필수 | 발표 시각 지연을 감안해 이전 base time 폴백 필요 |
| 한국환경공단 에어코리아 | PM10, PM2.5, 통합대기환경지수 | 조건부 | 도시 중심점과 측정소가 다름; 가까운 측정소 탐색 필요 |
| 기상청 자외선 관측/지수 | UV | 조건부 | 관측 지점 커버리지 검증 전에 도시 전체 예보로 단정 금지 |
| 한국천문연구원 출몰시각 정보 | 일출, 일몰 | 조건부 | 일 1회 캐시 가능; XML 응답 가능성 |

### 5.2 참고 소스

- [기상청 단기예보 조회서비스](https://www.data.go.kr/data/15084084/openapi.do)
- [기상청 API허브 동네예보](https://apihub.kma.go.kr/apiList.do?apiMov=4.+%EB%8F%99%EB%84%A4%EC%98%88%EB%B3%B4%28%EC%B4%88%EB%8B%A8%EA%B8%B0%EC%8B%A4%ED%99%A9%C2%B7%EC%B4%88%EB%8B%A8%EA%B8%B0%EC%98%88%EB%B3%B4%C2%B7%EB%8B%A8%EA%B8%B0%EC%98%88%EB%B3%B4%29+%EC%A1%B0%ED%9A%8C&seqApi=10&seqApiSub=286)
- [에어코리아 대기오염정보](https://www.data.go.kr/data/15073861/openapi.do)
- [기상청 자외선 관측](https://apihub.kma.go.kr/apiList.do?apiMov=%EC%9E%90%EC%99%B8%EC%84%A0%EA%B4%80%EC%B8%A1+%EC%A1%B0%ED%9A%8C&seqApi=2&seqApiSub=245)
- [한국천문연구원 출몰시각 정보](https://www.data.go.kr/data/15012688/openapi.do)

### 5.3 서울 MVP 위치

| 항목 | 계획값 |
| --- | --- |
| 도시 ID | `seoul` |
| 표시명 | `서울` |
| 기준 위치 | 서울시청 근처 `37.5665, 126.9780` |
| 기상청 동네예보 격자 | 초기 후보 `nx=60, ny=127`; 구현 전 공식 위경도↔격자 API/참고 파일로 재검증 |
| 대기질 | 서울시청에서 가까운 정상 가동 측정소를 위경도 기반으로 선택 |
| 타임존 | `Asia/Seoul` 고정 |

행정구역 대표점을 사용하므로 `서울 전체의 정확한 날씨`라고 단정하지 않고 `서울 중심 기준`으로 출처에 표시한다.

---

## 6. 시스템 아키텍처

```text
Vue WeatherCard
  └─ click(cityId)
      └─ WeatherDetailPanel
          └─ GET /api/weather/seoul
              └─ Weather Aggregator (server/serverless)
                  ├─ KMA current + forecast adapter
                  ├─ AirKorea adapter
                  ├─ KMA UV adapter
                  ├─ KASI sun-time adapter
                  ├─ validation + normalization
                  ├─ recommendation engine
                  └─ cache
```

### 6.1 프론트엔드 책임

- 카드 전체 클릭·키보드 입력 처리
- 패널 열기/닫기와 포커스 관리
- `idle/loading/success/partial/stale/error` 화면 상태
- 서버의 통합 데이터를 표시하되 외부 API 코드를 직접 해석하지 않음
- 캐릭터 레이어 또는 상태별 완성 자산 선택

### 6.2 서버/서버리스 책임

- 인증키 보관: `KMA_SERVICE_KEY`, `AIRKOREA_SERVICE_KEY`, `KASI_SERVICE_KEY`
- 발표 시각 계산, 이전 발표 시각 폴백
- 타임아웃·재시도·응답 검증·캐싱
- 외부 응답을 도메인 모델로 정규화
- 행동 추천과 캐릭터 상태 생성
- 부분 실패를 소스별 상태로 반환
- 개인 키·원본 오류 메시지를 클라이언트에 노출하지 않음

### 6.3 서버가 필요한 이유

Vite의 `VITE_*` 환경변수는 브라우저 번들에 포함되므로 비밀 정보가 아니다. 개발 프록시는 CORS를 완화할 뿐 운영에서 키를 보호하지 못한다. 따라서 운영을 가정한 MVP부터 API 호출은 서버 또는 Serverless Function을 통과한다.

---

## 7. 통합 API 계약

### 7.1 요청

```http
GET /api/weather/seoul
Accept: application/json
```

MVP에서는 임의 도시명을 받지 않고 허용 목록의 `cityId` 만 받아 SSRF·잘못된 위치 조회·예상치 못한 호출을 방지한다.

### 7.2 성공/부분 성공 응답

```json
{
  "schemaVersion": "1.0",
  "city": {
    "id": "seoul",
    "name": "서울",
    "basisLabel": "서울 중심 기준",
    "latitude": 37.5665,
    "longitude": 126.978
  },
  "status": "success",
  "fetchedAt": "2026-08-03T16:42:10+09:00",
  "observedAt": "2026-08-03T16:40:00+09:00",
  "current": {
    "temperatureC": 31,
    "humidityPct": 62,
    "windSpeedMs": 2.1,
    "precipitationMm1h": 0,
    "condition": "clear"
  },
  "hourly": [
    {
      "at": "2026-08-03T17:00:00+09:00",
      "temperatureC": 30,
      "sky": "clear",
      "precipitationType": "none",
      "precipitationProbabilityPct": 10,
      "precipitationMm1h": 0,
      "humidityPct": 60,
      "windSpeedMs": 2.4,
      "source": "kma-ultra-short"
    }
  ],
  "airQuality": {
    "stationName": "중구",
    "measuredAt": "2026-08-03T16:00:00+09:00",
    "pm10UgM3": 25,
    "pm25UgM3": 12,
    "grade": "good"
  },
  "uv": {
    "value": 7,
    "grade": "high",
    "measuredOrForecastAt": "2026-08-03T15:00:00+09:00",
    "stationName": "서울",
    "coverage": "station"
  },
  "sun": {
    "date": "2026-08-03",
    "sunrise": "05:37",
    "sunset": "19:37"
  },
  "advice": {
    "primaryReason": "uv",
    "headline": "오늘은 햇빛을 단단히 막아요",
    "description": "선크림과 양산을 챙겨주세요",
    "outfit": ["breathable-top", "summer-bottoms", "sun-hat"],
    "items": ["sunscreen", "parasol", "water"],
    "mood": "bright",
    "altText": "여름 옷과 모자를 입고 양산과 물을 챙긴 캐릭터"
  },
  "sources": {
    "weather": { "state": "fresh", "provider": "KMA" },
    "airQuality": { "state": "fresh", "provider": "AirKorea" },
    "uv": { "state": "fresh", "provider": "KMA" },
    "sun": { "state": "fresh", "provider": "KASI" }
  },
  "warnings": []
}
```

### 7.3 응답 상태

| `status` | 의미 | HTTP |
| --- | --- | --- |
| `success` | 필수 및 조건부 데이터 정상 | 200 |
| `partial` | 기상청 핵심 데이터는 있지만 보조 소스 일부 실패 | 200 |
| `stale` | 최신 호출 실패, TTL을 넘지 않은 이전 성공 자료 사용 | 200 |
| `error` | 핵심 날씨를 제공할 수 없음 | 502 또는 503 |
| `not-found` | 허용되지 않은 도시 ID | 404 |

### 7.4 null 규칙

- 숫자 `0`은 값이며 `null`로 변환하지 않는다.
- `-`, 빈 문자열, 측정 중지, 비정상 범위는 `null`로 정규화한다.
- `null` 항목으로 위험 없음을 판정하지 않는다.
- 데이터가 없으면 UI에서 `0`, `좋음`, `비 없음`으로 표시하지 않는다.

---

## 8. 기상 데이터 정규화

### 8.1 핵심 카테고리

| 원본/개념 | 도메인 필드 | 처리 |
| --- | --- | --- |
| 기온 | `temperatureC` | `Number`, 합리적 범위 `-50..60` 이외 null |
| 습도 | `humidityPct` | `0..100` |
| 풍속 | `windSpeedMs` | `>=0`; 문자열 숫자 변환 |
| 하늘상태 SKY | `clear/cloudy/overcast` | 알 수 없는 코드는 `unknown` |
| 강수형태 PTY | `none/rain/sleet/snow/raindrop/...` | 코드 테이블을 하나의 adapter에서만 관리 |
| 강수확률 | `precipitationProbabilityPct` | `0..100` |
| 1시간 강수량 PCP | `precipitationMm1h` | `강수없음` 등 정성 문자열을 0 또는 범위로 파싱 |

### 8.2 강수 구간 생성

1. 시간별 데이터를 KST 오름차순으로 정렬한다.
2. `PTY != none` 또는 `PCP > 0` 또는 `POP >= 60`을 비 후보로 판정한다.
3. 연속된 후보를 하나의 구간으로 묶는다.
4. 시작 시각, 마지막 비 시각의 1시간 뒤를 종료 시각, 최대 확률, 누적/최대 시간당 강수량을 생성한다.
5. 확률만 높고 PTY/PCP가 없으면 `비 가능성`, PTY 또는 PCP가 있으면 `비 예상`으로 문구 강도를 달리한다.

### 8.3 시간 병합

- 현재~6시간: 초단기예보를 우선한다.
- 6시간 이후: 단기예보를 사용한다.
- 같은 시간이 겹치면 초단기 값으로 덮어쓴다.
- 발표 시각이 더 오래된 자료가 더 최신 자료를 덮어쓰지 못하게 한다.
- 중복·역순·파싱 불가 시간은 제거하고 warning을 기록한다.

---

## 9. 행동 추천·캐릭터 규칙

### 9.1 우선순위

```text
기상특보 > 많은 비/눈 > 폭염/한파 > 강풍
> 일반 비/눈 > 대기질 > UV > 평온한 날씨
```

우선순위 1위만 헤드라인이 되며, 2~3위는 소품 칩 또는 보조 문구로 표시한다.

### 9.2 MVP 규칙표

| ID | 조건 | 행동 문구 | 아웃핏 | 소품 |
| --- | --- | --- | --- | --- |
| ADV-R01 | 6시간 이내 PTY가 비/진눈깨비 | `{start}시 전에 우산을 챙겨요` | `rain-jacket`, `rain-shoes` | `umbrella` |
| ADV-R02 | 최대 시간당 PCP `>= 15mm` | `비가 강해요. 이동 시간을 여유 있게 잡아요` | `raincoat`, `rain-boots` | `large-umbrella` |
| ADV-H01 | 현재/2시간 최고 기온 `>= 30°C` | `더위에 지치지 않게 물을 챙겨요` | `breathable-top`, `summer-bottoms` | `water` |
| ADV-C01 | 현재/2시간 최저 기온 `< 5°C` | `따뜻하게 입어요` | `warm-coat`, `scarf` | `gloves` |
| ADV-U01 | UV `3..7` | `자외선 차단을 준비해요` | `sun-hat` | `sunscreen` |
| ADV-U02 | UV `>= 8` | `한낮 햇빛을 피하고 차단을 꼼꼼히 해요` | `sun-hat`, `light-long-sleeve` | `sunscreen`, `parasol` |
| ADV-A01 | AirKorea 등급 `bad` 이상 | `오래 하는 야외 활동을 줄여요` | `default-seasonal` | `mask` |
| ADV-D01 | 위 조건 없음 | `큰 날씨 걱정 없이 외출하기 좋아요` | `default-seasonal` | 없음 |

UV 권고는 WHO의 `UVI 3 이상에서 보호 권장`을 제품 문구의 기본으로 삼는다. 이 문구는 의학적 처방이 아니며, 데이터가 없으면 권고를 생성하지 않는다.

### 9.3 규칙 충돌 예시

| 입력 | 대표 상태 | 보조 상태 |
| --- | --- | --- |
| 많은 비 + UV 높음 | 비 | UV 칩은 현재 비가 오면 숨김 |
| 폭염 + UV 높음 | 폭염 | 모자·선크림·양산 추가 |
| 미세먼지 나쁨 + 비 임박 | 비 | 마스크 칩 추가 |
| 강풍 + 비 | 강풍/특보 | 양산 제외, 우산 사용 주의 문구 |

### 9.4 캐릭터 자산 전략

MVP에서는 런타임에 AI 이미지를 생성하지 않는다. 미리 검수한 상태별 WebP/PNG 자산 또는 분할 레이어를 사용한다.

- 1차: `default`, `sunny-hot`, `rain`, `cold`, `air-bad` 5개 완성 자산
- 2차: 몸·상의·하의·외투·소품 레이어로 확장
- 모든 자산은 동일한 캔버스, 포즈, 비율, 광원을 사용
- 자산 실패 시 이미지 대신 의복 텍스트를 제공

---

## 10. 캐시, 타임아웃, 재시도

| 소스 | fresh TTL | stale 허용 | 타임아웃 | 재시도 |
| --- | --- | --- | --- | --- |
| 기상청 실황 | 10분 | 60분 | 3초 | 1회, 200~500ms jitter |
| 기상청 예보 | 30분 | 3시간 | 4초 | 1회 |
| 에어코리아 | 30분 | 2시간 | 3초 | 1회 |
| UV | 60분 | 3시간 | 3초 | 1회 |
| 일출·일몰 | 해당 날 자정까지 | 7일 | 3초 | 1회 |

- 4xx(잘못된 키·요청)는 재시도하지 않는다.
- 429, 5xx, 네트워크 타임아웃만 한 번 재시도한다.
- 같은 도시의 동시 요청은 하나의 진행 중 Promise로 결합한다.
- stale 데이터를 사용하면 `observedAt` 및 `이전 정보` 배지를 반드시 표시한다.

---

## 11. 예상 오류와 설계 대응

| ID | 예상 오류 | 영향 | 예방/대응 | UI |
| --- | --- | --- | --- | --- |
| E01 | 발표 직후 최신 base time으로 조회했지만 아직 자료 없음 | 예보 빈 목록 | 일정 지연 여유를 두고 직전 발표 시각으로 1회 폴백 | 자동 복구 시 표시 없음 |
| E02 | 자정 근처 base date가 하루 잘못됨 | 전체 실패/어제 예보 | `Asia/Seoul` 시계와 테이블 기반 base time 계산 | 최종 실패만 안내 |
| E03 | 서비스키 인코딩 중복 | 인증 실패 | encoded/decoded 키 중 하나만 환경변수 규약으로 확정 | 관리자 로그, 사용자에게 일반 오류 |
| E04 | API 응답은 HTTP 200이지만 본문 결과코드가 오류 | 정상으로 오판 | HTTP와 body result code 모두 검증 | 재시도/이전 정보 |
| E05 | JSON 요청에 XML 오류 응답 | JSON parse 예외 | content-type과 원문 헤더를 검증, XML 오류 파서 | 일반 오류 |
| E06 | PCP가 `강수없음`, `1mm 미만` 등 문자열 | NaN/그래프 붕괴 | 명시적 파서와 범위 형식 | 정성 표시 또는 범위 |
| E07 | `0`을 falsy로 취급 | 강수 0, 풍속 0 누락 | `value == null`로만 결측 판정 | `0` 정상 표시 |
| E08 | 초단기·단기 중복 시간 | 타임라인 중복/역순 | ISO KST key로 merge, 초단기 우선 | 정상 표시 |
| E09 | 경계 시간에 예보가 끊김 | 비 종료 오판 | 없는 구간을 `비 종료`로 해석하지 않음 | `~20시까지 확인됨` |
| E10 | 측정소 값 `-`, 점검 중 | 미세먼지 `좋음` 오판 | null + source warning | `측정 정보 없음` |
| E11 | 가까운 측정소가 오프라인 | 오래된 AQI | 거리 + 최신 측정시각으로 다음 측정소 선택 | 측정소·시각 표시 |
| E12 | UV 관측소가 서울 대표성이 낮음 | 잘못된 양산 권고 | coverage를 표시, 거리/시각 허용치 초과 시 null | UV 숨김/확인 불가 |
| E13 | 일출·일몰 XML 파싱/지역명 불일치 | 일출 누락 | 위경도 기반 조회 우선, 일 단위 캐시 | 항목만 숨김 |
| E14 | 호출량 제한/429 | 전체 지연 | 서버 캐시, in-flight dedupe, 1회 백오프 | stale 표시 |
| E15 | CORS | 브라우저에서 직접 호출 실패 | 서버 통합 API만 호출 | 해당 없음 |
| E16 | 브라우저 번들에 API 키 포함 | 키 유출/사용량 탈취 | `VITE_*` 금지, 서버 secret | 해당 없음 |
| E17 | 빠른 도시 전환으로 느린 응답이 덮어씀 | 잘못된 도시 표시 | AbortController + request ID 검증 | 최종 선택만 표시 |
| E18 | 패널을 닫은 후 응답 도착 | 닫힌 UI 변경/메모리 누수 | 요청 취소 또는 결과만 캐시 | 화면 재오픈 없음 |
| E19 | 캐릭터 이미지 404/느린 로드 | 히어로 공백/레이아웃 쉬프트 | 고정 aspect-ratio, fallback, 자산 preload | 대체 텍스트 |
| E20 | 규칙 충돌로 우산+양산 등 모순 소품 | UX 신뢰 하락 | 우선순위·상호배제 테이블 | 최대 3개만 표시 |
| E21 | 캐시에 날짜가 지난 일출 정보 | 오늘 시각 오표시 | cache key에 KST 날짜 포함 | 정상 갱신 |
| E22 | 서버 시계가 UTC | 잘못된 base date/time | 도메인 계산에 `Asia/Seoul` 명시 | 정상 표시 |

---

## 12. 보안·운영·관찰성

### 12.1 보안

- API 키는 서버 secret으로만 관리하고 로그에서 마스킹한다.
- `cityId`는 허용 목록으로 검증한다.
- 외부 API 원문을 그대로 사용자에게 반환하지 않는다.
- 단일 클라이언트의 과도한 새로고침은 서버 rate limit으로 보호한다.
- 위치는 MVP에서 고정 도시 좌표만 사용하며 개인 위치를 수집하지 않는다.

### 12.2 관찰 항목

| metric/log | 목적 |
| --- | --- |
| 소스별 latency | 느린 제공자 파악 |
| 소스별 success/partial/failure 비율 | 장애 파악 |
| cache hit/stale hit 비율 | 호출량·신선도 튀닝 |
| selected base time·fallback 여부 | 발표 지연 파악 |
| recommendation rule ID | 잘못된 문구 추적 |
| 정규화 warning 수 | API 형식 변경 감지 |

로그에는 서비스키, 전체 원문 응답, 개인 식별 정보를 남기지 않는다.

---

## 13. 구현 단계 계획

### Phase 0. 외부 조건 확정

1. 기상청, 에어코리아, 한국천문연구원 활용 신청
2. 실제 서울 응답 샘플을 각각 한 번 저장
3. 서울 격자, 대기질 측정소, UV 관측소 적합성 검증
4. 응답 형식, 이용한도, 저작권·출처 표시 조건 기록

**완료 조건:** 서울 호출 샘플과 필드 매핑표가 리뷰됨.

### Phase 1. TDD 기반 도메인 로직

1. Vitest 설정
2. base time 계산 테스트를 먼저 작성
3. KMA/AirKorea/KASI fixture 파서 테스트 작성
4. 시간별 merge·강수 구간·권고 규칙 테스트 작성
5. 최소 코드로 통과 후 refactor

**완료 조건:** 외부 네트워크 없이 모든 도메인 테스트가 결정적으로 통과.

### Phase 2. 서버 통합 API

1. adapter interface와 통합 응답 스키마 구현
2. timeout, retry, cache, stale fallback
3. 부분 성공·오류 매핑
4. mock server 기반 contract test
5. 실제 키를 사용한 수동 smoke test

**완료 조건:** `/api/weather/seoul`이 통합 계약과 부분 장애 요구사항을 충족.

### Phase 3. Vue 상세 UI

1. `WeatherDetailPanel` 및 화면 상태 구현
2. 도시 카드 전체 클릭, 삭제 버튼 버블링 방지
3. 캐릭터 히어로, 준비물, 타임라인, 지표
4. 로딩·부분 성공·stale·오류 화면
5. 포커스·키보드·reduced motion

**완료 조건:** 서울 카드에서 주요 사용자 흐름과 접근성 테스트 통과.

### Phase 4. 자산·통합·회귀 검증

1. 5개 캐릭터 상태 자산 최적화
2. 응답 순서 경쟁, 장애, 느린 네트워크 검증
3. 기존 검색·필터·정렬·삭제 회귀 테스트
4. 모바일/데스크톱 시각 QA
5. 출처, 업데이트 시각, 오류 문구 검수

**완료 조건:** MVP 수용 기준 전체 통과.

---

# 14. TDD — 서울 MVP 테스트 명세

## 14.1 TDD 운영 원칙

이 문서 단계에서는 테스트 코드를 작성·실행하지 않고, 다음 구현 단계에서 쓸 실패 테스트와 fixture를 먼저 확정한다.

```text
Red: 해당 요구사항을 표현하는 실패 테스트 작성
Green: 가장 작은 구현으로 통과
Refactor: 통합 계약·규칙 우선순위를 유지하며 구조 개선
```

### 예정 테스트 도구

| 계층 | 도구 |
| --- | --- |
| 단위/컴포넌트 | Vitest + Vue Test Utils |
| HTTP mock | MSW 또는 adapter fake; 하나로 확정 |
| DOM 접근성 | Testing Library 쿼리 우선, axe 추가 검토 |
| E2E | Playwright(구현 시 프로젝트 환경에 맞춰 최종 확정) |

### 14.2 fixture 목록

```text
tests/fixtures/seoul/
  kma-current-clear.json
  kma-current-rain.json
  kma-ultra-rain-17-to-20.json
  kma-short-clear-hot.json
  kma-no-data.json
  kma-error-xml.xml
  airkorea-good.json
  airkorea-bad.json
  airkorea-maintenance.json
  uv-high.json
  uv-missing.json
  kasi-sun-times.xml
  expected-clear-domain.json
  expected-rain-domain.json
```

fixture에는 서비스키·실제 사용자 정보를 넣지 않는다. 실제 응답 샘플을 사용하더라도 키와 불필요한 메타데이터를 제거한다.

## 14.3 단위 테스트

### 시간·격자

| ID | Given | When | Then |
| --- | --- | --- | --- |
| UT-TIME-01 | KST 2026-08-03 16:42 | 최신 초단기 base time 계산 | 지연 여유를 반영한 이용 가능 시각 반환 |
| UT-TIME-02 | KST 2026-08-03 00:05 | base date/time 계산 | 필요 시 2026-08-02 발표 시각 반환 |
| UT-TIME-03 | 서버 환경이 UTC | KST 발표 시각 계산 | UTC 서버에서도 같은 결과 |
| UT-GRID-01 | 서울 기준 위경도 | 격자 변환 | 공식 참고값과 일치 |
| UT-GRID-02 | 지원 범위 밖 좌표 | 격자 변환 | typed validation error |

### 파싱·정규화

| ID | Given | When | Then |
| --- | --- | --- | --- |
| UT-PARSE-01 | 기온 `31`, 습도 `62`, 풍속 `0` | KMA 파서 | 31, 62, 0을 누락 없이 반환 |
| UT-PARSE-02 | PCP `강수없음` | 강수량 파서 | 0mm + qualitative original metadata |
| UT-PARSE-03 | PCP `1mm 미만` | 강수량 파서 | 범위/upper bound를 정책대로 반환, NaN 금지 |
| UT-PARSE-04 | 알 수 없는 SKY/PTY 코드 | 파서 | `unknown` + warning |
| UT-PARSE-05 | AirKorea PM10 `-` | 파서 | null, `good` 금지 |
| UT-PARSE-06 | HTTP 200 + 오류 resultCode | adapter | failure로 분류 |
| UT-PARSE-07 | XML 오류 본문 | JSON adapter | 파싱 예외를 provider error로 매핑 |

### 시간별 merge·강수 구간

| ID | Given | When | Then |
| --- | --- | --- | --- |
| UT-MERGE-01 | 17~20시 초단기, 17~23시 단기 | merge | 17~20시는 초단기, 이후는 단기 |
| UT-MERGE-02 | 중복·역순 입력 | merge | 중복 없는 오름차순 |
| UT-RAIN-01 | 17~19시 PTY=rain, 20시 none | 구간 생성 | 시작 17:00, 종료 20:00 |
| UT-RAIN-02 | 17시 POP 80, PTY none, PCP 0 | 구간 생성 | `비 가능성`, `비 확정` 금지 |
| UT-RAIN-03 | 18시 PCP 15 | 강우 규칙 | ADV-R02 발동 |
| UT-RAIN-04 | 마지막 자료가 20시 rain | 구간 생성 | 종료 단정 금지, `20시까지 확인` |

### 추천 엔진

| ID | Given | When | Then |
| --- | --- | --- | --- |
| UT-ADV-01 | 비 없음, 31°C, UV 7 | 추천 생성 | 더위가 primary, 모자·선크림·물 표시 |
| UT-ADV-02 | 17시 비, UV 8 | 추천 생성 | 비가 primary, 우산 표시, 양산 제외 |
| UT-ADV-03 | 비 + 미세먼지 나쁨 | 추천 생성 | 우산 + 마스크, 최대 3개 |
| UT-ADV-04 | UV null | 추천 생성 | UV 권고 생성 금지 |
| UT-ADV-05 | 모든 위험 조건 없음 | 추천 생성 | ADV-D01 |
| UT-ADV-06 | 같은 입력을 두 번 실행 | 추천 생성 | 완전히 같은 결과 |

## 14.4 서버 통합·계약 테스트

| ID | 시나리오 | 기대 결과 |
| --- | --- | --- |
| IT-API-01 | 모든 provider 정상 | HTTP 200, `status=success`, schema 1.0 |
| IT-API-02 | KMA current 성공, AirKorea 실패 | HTTP 200, `status=partial`, 핵심 날씨 표시 |
| IT-API-03 | KMA 최신 base time 데이터 없음 | 직전 base time으로 폴백, warning 기록 |
| IT-API-04 | KMA 실패 + 유효한 stale cache | HTTP 200, `status=stale`, 이전 시각 표시 |
| IT-API-05 | KMA 실패 + cache 없음 | HTTP 503, `status=error` |
| IT-API-06 | `GET /api/weather/busan` in Seoul-only MVP | HTTP 404 |
| IT-API-07 | 같은 서울 요청 10개 동시 발생 | provider 호출은 소스별 1회 |
| IT-API-08 | provider 429 | 1회 재시도 후 stale 또는 partial |
| IT-API-09 | 응답에 서비스키/원문 오류 포함 여부 | 비밀 정보 없음 |
| IT-API-10 | 통합 응답 fixture | JSON Schema 검증 통과 |

## 14.5 Vue 컴포넌트 테스트

| ID | Given | When | Then |
| --- | --- | --- | --- |
| CT-UI-01 | 서울 카드 | 카드 본문 클릭 | 서울 상세 패널 열림, API 호출 1회 |
| CT-UI-02 | 서울 카드 | `Enter` | 상세 패널 열림 |
| CT-UI-03 | 서울 카드 | 삭제 버튼 클릭 | 삭제만 발생, 패널은 열리지 않음 |
| CT-UI-04 | 패널 열림 | `Esc` | 패널 닫힘, 서울 카드로 포커스 복귀 |
| CT-UI-05 | loading | 응답 대기 | 스켈레톤, 이전 도시 데이터 노출 없음 |
| CT-UI-06 | success sunny fixture | 렌더 | 더위/UV 캐릭터·문구·타임라인 일치 |
| CT-UI-07 | success rain fixture | 렌더 | 방수 아웃핏, 17시 우산 문구, 강수 구간 일치 |
| CT-UI-08 | partial, AirKorea 실패 | 렌더 | 날씨는 표시, 대기질만 `정보 없음` |
| CT-UI-09 | stale | 렌더 | `이전 정보` 배지 + 기준 시각 |
| CT-UI-10 | error | 렌더 | 중립 캐릭터, 재시도 버튼, 가짜 숫자 없음 |
| CT-UI-11 | 캐릭터 이미지 실패 | error event | 대체 텍스트, 레이아웃 유지 |
| CT-UI-12 | reduced motion | 렌더 | 자동 애니메이션 없음 |

## 14.6 E2E 사용자 흐름

### E2E-01 서울 맑고 더운 날

```gherkin
Given 서울 날씨 API가 31°C, 맑음, UV 7을 반환하고
And 대기질은 좋음이고
When 사용자가 서울 카드 전체를 누르면
Then 상세 패널이 열린다
And 여름 옷·모자 캐릭터가 보인다
And 물·선크림 준비물이 보인다
And 출처와 데이터 기준 시각이 보인다
```

### E2E-02 서울 퇴근길 비

```gherkin
Given 서울에 17시부터 20시까지 비가 예보되고
And 18시 예상 강수량이 가장 높고
When 사용자가 서울 카드를 누르면
Then 방수 재킷과 우산을 준비한 캐릭터가 보인다
And "17시 전에 우산을 챙겨요"가 보인다
And 17~20시 강수 구간과 18시 peak가 일치한다
```

### E2E-03 미세먼지 API 부분 장애

```gherkin
Given 기상청 날씨는 정상이고
And 에어코리아가 타임아웃이고
When 사용자가 서울 상세를 열면
Then 현재 날씨와 시간별 예보는 보인다
And 미세먼지에만 "정보를 불러오지 못했어요"가 보인다
And 미세먼지를 근거로 한 마스크 권고는 없다
```

### E2E-04 최신 조회 실패·이전 데이터

```gherkin
Given 최신 날씨 호출은 실패하고
And 30분 전 성공 캐시가 있고
When 사용자가 서울 상세를 열면
Then 캐시된 날씨가 보인다
And "이전 정보"와 30분 전 기준 시각이 보인다
And 새로고침 버튼이 보인다
```

## 14.7 MVP 수용 기준

### 기능

- [ ] 서울 카드 전체 클릭·`Enter`·`Space`로 상세가 열린다.
- [ ] 삭제 버튼이 상세 열기를 발생시키지 않는다.
- [ ] 최신 현재 기온·습도·풍속·강수와 최소 12시간 예보가 보인다.
- [ ] 비 시작 시각·강수확률·강수량이 구분된다.
- [ ] 행동 문구와 캐릭터 아웃핏이 같은 규칙 결과에서 나온다.
- [ ] 출처, 관측/발표 시각, 앱 갱신 시각이 구분된다.
- [ ] 보조 소스 실패가 핵심 날씨를 숨기지 않는다.

### 품질

- [ ] 통합 API 캐시 hit 시 서버 응답 p95가 개발 환경 기준 500ms 이하이다.
- [ ] cache miss의 provider 호출은 각 timeout 범위를 넘지 않는다.
- [ ] 포커스 트랩·복귀, 대체 텍스트, 키보드 조작이 통과한다.
- [ ] 캐릭터 자산 실패가 layout shift나 기능 실패를 만들지 않는다.
- [ ] 프론트엔드 번들·네트워크 응답·로그에 API 키가 노출되지 않는다.
- [ ] 필수 단위·통합·컴포넌트·E2E 테스트가 통과한다.

---

## 15. 요구사항-테스트 추적표

| 요구사항 | 주요 테스트 |
| --- | --- |
| 카드 전체 상세 진입 | CT-UI-01~04 |
| 서울 격자·KST 발표 시각 | UT-TIME-01~03, UT-GRID-01 |
| KMA 응답 정규화 | UT-PARSE-01~07 |
| 시간별 예보·강수 구간 | UT-MERGE-01~02, UT-RAIN-01~04 |
| 아웃핏·행동 추천 | UT-ADV-01~06, E2E-01~02 |
| 부분 장애 | IT-API-02, CT-UI-08, E2E-03 |
| stale fallback | IT-API-04, CT-UI-09, E2E-04 |
| 보안·키 보호 | IT-API-09 |
| 접근성·자산 fallback | CT-UI-02, CT-UI-04, CT-UI-11~12 |

---

## 16. 구현 전 미해결 검증 항목

아래는 스펙을 변경하는 선택이 아니라 실제 API로 확인해야 하는 항목이다.

1. 서울 대표 격자 `60,127`의 공식 최신 매핑 재확인
2. 개발에 사용할 기상청 API허브와 공공데이터포털 단기예보 중 주 경로 확정
3. PCP 정성 값의 최신 표현 목록과 단위
4. 서울 자외선 관측 지점의 공간·시간 커버리지
5. 서울시청 기준 에어코리아 측정소 후보와 점검 시 대체 순위
6. 한국천문연구원 응답 형식·일일 호출 제한
7. 공공데이터 출처 표시 문구와 이용허락 요구사항
8. 교육용 로컬 MVP와 배포 가능 MVP 중 서버리스 실행 환경 선택

이 항목은 Phase 0에서 실제 응답 샘플과 함께 확정하고, 변경된 내용은 이 문서의 데이터 계약·fixture·테스트에 동시 반영한다.

---

## 17. Definition of Done

MVP는 다음을 모두 만족할 때 완료로 판정한다.

1. Phase 0의 외부 API 검증 결과가 문서와 fixture에 반영되었다.
2. 서울 카드 전체 클릭으로 상세 패널을 열고 닫을 수 있다.
3. 현재 날씨, 최소 12시간 예보, 강수 구간이 통합 계약에 맞게 표시된다.
4. 행동 헤드라인·준비물·캐릭터 아웃핏이 같은 권고 규칙 결과와 일치한다.
5. 부분 장애, stale cache, 전체 장애가 구분된다.
6. 출처와 데이터 기준 시각이 보인다.
7. 보안·접근성·회귀 테스트를 포함한 필수 TDD 명세가 통과한다.
8. 기존 도시 검색·필터·정렬·추가·삭제 기능에 회귀가 없다.
